---
name: engineering-patterns
description: LJ 的跨项目工程判断。当需要决定并发形态、agent 职责划分、评分/评估设计、模型选择、或项目收尾交付形式时使用。
---
<!-- 唯一正本。~/project-template/PATTERNS.md 是指向本文件的符号链接；项目内的 PATTERNS.md 是 cp 快照，故意不同步。只改这里。 -->

## 并发
- 扇出+隔离+汇总：仅当子任务独立、各需不同数据、合并逻辑简单时用
- 有依赖就串行。反例：简历→投递→内推，每步吃掉上一步结果，不能并行

## 复用
- 固化"怎么求值"，不固化值：步骤存 source 描述符（profile.key / job.key / 模板），重放时对新上下文
  重新 resolve —— 在 A 家学到的填表方法，换 B 家原样能用
- 捕获时的定位符是易腐的（元素 id 每个实例都在变），语义标签才是持久 key；重放按标签重新定位，
  定位不到就跳过，不要等超时
- 方法要能长大：重放遇到更复杂的实例时只对缺口调模型，再把新步骤折回去重新固化 ——
  不是命中/未命中的二值缓存
- 判据：这次推理的产物，下次能不能不调模型就用上。不能就只是缓存

## 人在回路
- 该由人拍板的事（花钱、让步、超出授权范围），agent 不自己决定也不终止：给一句占位回应，
  把决策做成结构化问题 + 2-4 个选项，挂起等人
- 挂起 ≠ 空闲：等人的状态必须对超时监控显式声明自己活着（每轮刷新 last_touch），
  否则正常等待会被看门狗当成卡死回收
- 等待本身要有超时，超时后交还给人而不是替人默认

## 评估与裁决
- 执行者与评估者隔离，只让没提出该发现的一方投票
- 判定规则机械化：两票确认 / 一票带反对意见交付 / 零票丢弃
- 最终裁决用确定性规则，不让模型讨论出结论
- 不变量落在代码校验层，prompt 只做解释：写进 prompt 的是建议，写进入参 schema 的才是约束。
  设计时先问"这条违反了会怎样"，能变成一个 400 的就别留在 prompt 里
- 空输出 ≠ 没价值：pass / fail / no_evidence 三态，失败和"确实没内容"分开计
- 零产出 ≠ 负面证据：调用失败和完全无输出都是无信息状态（分不清是挂了还是真没有）；
  只有"确实产出了但一条都没被采纳"才算负面。裁撤/降权只能建立在后者上
- 守卫：不允许全裁 —— 要把所有输入判死时，先怀疑规则本身
- 区分"空"和"坏"：维护良性错误白名单（表不存在 / 无数据 → 返回空），不在白名单的照常抛。
  不要 try/except pass
- 多路证据取并集而非要求一致；分歧记成遥测保持可见，不要变成异常
- 同一判据在已知答案和未知答案两种场景下口径本就该不同：跑金标集时"一条都没报"= 故障，
  跑真实输入时"一条都没报"= 无信息。别用同一个阈值
- 同一输入跑 K 次，K 次 verdict 不一致就标 unstable；不稳定的 pass 不算 pass
- 判据误伤不要靠加关键词补（这次补了下次换个说法还是误伤），语义判断归 LLM 层，规则层标 deferred 不计分
- 子串匹配无法区分肯定与否定（"承诺赔偿" vs "不能承诺赔偿"），这是判据的能力边界，不是关键词列表不全
- LLM 评审必须逐字引用原文，且引文能被代码回查；引不出来的票作废 —— 不然 judge 会编证据
- 金标集（植入已知错误算召回率）优于人工判断；除植入缺陷外还要有干净对照块，报在上面的算误报 ——
  召回率之外要有误报率
- 对照组不能被削弱：同样输入、同样模型、同样辅助资源，唯一变量是你要证明的那一个
  （单 agent 对照组要拿到全部专业领域的参考资料，只是串行做）
- 性能/并发主张的证据取自被测系统外部的时间线（引擎自己的 enter/leave 事件），不取自 agent 自述
  —— agent 会"叙述"它做了并发
- gate 通过只说明 rubric 在金标集覆盖的形状上正确，不等于没问题；真实 run 里的可疑判定要回灌成新 case
- 单维度优化会伤到没被测量的维度（要求首句直接回答 → 道歉被挤出首句），改完要全维度重测
- judge 必须知道被测系统的行为契约，否则会把"按要求做的事"判成违规；契约放 judge 的共享 prompt，
  不放单个维度 —— 逐维度打补丁的话，修好一个，同样的误判会在另一个维度冒出来

## 模型选择
- 能用代码判断的不调模型
- 抽取/分类/格式转换 → Haiku
- 理解上下文、判断语气顺序 → Sonnet
- 安全、多步推理、错了代价高 → Opus

## 数据
- 开工前先检查输入可信度，数据有问题就停，不要基于坏数据分析
- manifest 存输入文件 sha，输入变了就知道旧结果不能再拿来对比

## 收尾
- 每个项目录 3 分钟 demo video + README 放 2-3 张截图
- 前端只在输出需要交互或可视化时做。判据：输出是不是需要"看"的
- 演示路径 ≠ 主张路径时，在交付物里显式说明替换了什么、为什么 —— 能跑的 demo 很容易暗示一个
  它其实没支撑的主张，这种暗示会在别人复现时才暴露
- 逐项列"声称用了 X"和"X 实际承担了什么"；只验证没执行、只审计没安装，都照实写
